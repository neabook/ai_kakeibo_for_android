name: ai_kakeibo
description: "AI家計簿アプリ - レシートをAIで自動解析"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 状態管理
  flutter_riverpod: ^2.6.1

  # ローカルDB (Drift)
  drift: ^2.22.1
  sqlite3_flutter_libs: ^0.5.28
  path_provider: ^2.1.5
  path: ^1.9.1

  # UI/グラフ
  fl_chart: ^0.69.2

  # ユーティリティ
  intl: ^0.20.1
  uuid: ^4.5.1

  # Phase 2: OCR機能
  image_picker: ^1.1.2
  http: ^1.2.2

  # Phase 4: 収益化
  google_mobile_ads: ^5.2.0
  purchases_flutter: ^8.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # Driftコード生成
  drift_dev: ^2.22.1
  build_runner: ^2.4.14

flutter:
  uses-material-design: true

  # アセット（将来使用）
  # assets:
  #   - assets/images/
